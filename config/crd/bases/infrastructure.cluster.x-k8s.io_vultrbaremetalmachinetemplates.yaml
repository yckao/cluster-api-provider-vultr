---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: vultrbaremetalmachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: VultrBareMetalMachineTemplate
    listKind: VultrBareMetalMachineTemplateList
    plural: vultrbaremetalmachinetemplates
    shortNames:
    - vbmmt
    singular: vultrbaremetalmachinetemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VultrBareMetalMachineTemplateSpec defines the desired state
              of VultrBareMetalMachineTemplate
            properties:
              template:
                description: VultrBareMetalMachineTemplateResource describes the data
                  needed to create a VultrBareMetalMachine from a template.
                properties:
                  spec:
                    description: Spec is the specification of the desired behavior
                      of the bare metal machine.
                    properties:
                      app_id:
                        description: AppID is the Vultr marketplace application ID
                          to deploy on the bare metal server.
                        type: integer
                      image_id:
                        description: ImageID is the Vultr image ID to use when deploying
                          the bare metal server.
                        type: string
                      ipxe_chain_url:
                        description: IPXEChainURL is the URL to chain-load iPXE from
                          when booting the bare metal server.
                        type: string
                      mdisk_mode:
                        description: MdiskMode is the RAID configuration for bare
                          metal servers with multiple disks.
                        type: string
                      os_id:
                        description: OsID is the Vultr OS ID to use when deploying
                          the bare metal server.
                        type: integer
                      persistent_pxe:
                        description: |-
                          PersistentPxe enables persistent PXE booting for the bare metal server.
                          When true, the server will always boot from PXE.
                        type: boolean
                      planID:
                        description: PlanID is the id of Vultr bare metal plan.
                        type: string
                      providerID:
                        description: ProviderID is the unique identifier as specified
                          by the cloud provider.
                        type: string
                      region:
                        description: The Vultr Region (DCID) the cluster lives on.
                        type: string
                      reserved_ipv4:
                        description: ReservedIPv4 is the reserved IPv4 address to
                          assign to the bare metal server.
                        type: string
                      reservedIPRef:
                        description: ReservedIPRef is a reference to a VultrReservedIP
                          CR in the same namespace. Takes precedence over ReservedIPv4.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      script_id:
                        description: StartupScriptID is the Vultr startup script ID
                          to execute on the bare metal server.
                        type: string
                      sshKey:
                        description: sshKey is the name of the ssh key to attach to
                          the bare metal server.
                        items:
                          type: string
                        type: array
                      startupScriptRef:
                        description: StartupScriptRef is a reference to a VultrStartupScript
                          CR in the same namespace. Takes precedence over StartupScriptID.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      vpc_id:
                        description: |-
                          VPCID is the id of the VPC to be attached after creation.
                          Note: Bare metal VPC attachment is performed after server creation.
                        type: string
                      vpcRef:
                        description: VPCRef is a reference to a VultrVPC CR in the
                          same namespace. Takes precedence over VPCID.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - region
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
