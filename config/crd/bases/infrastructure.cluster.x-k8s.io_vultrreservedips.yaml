---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: vultrreservedips.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: VultrReservedIP
    listKind: VultrReservedIPList
    plural: vultrreservedips
    singular: vultrreservedip
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: ReservedIP is ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Vultr ReservedIP ID
      jsonPath: .status.id
      name: ID
      type: string
    - description: Assigned IP address
      jsonPath: .status.subnet
      name: Subnet
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VultrReservedIP is the Schema for the vultrreservedips API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VultrReservedIPSpec defines the desired state of VultrReservedIP.
            properties:
              ipType:
                description: 'IPType is the IP type: "v4" or "v6".'
                type: string
              label:
                description: Label is an optional label for the reserved IP.
                type: string
              region:
                description: Region is the Vultr region for the reserved IP.
                type: string
            required:
            - ipType
            - region
            type: object
          status:
            description: VultrReservedIPStatus defines the observed state of VultrReservedIP.
            properties:
              conditions:
                description: Conditions defines current service state of the VultrReservedIP.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              id:
                description: ID is the Vultr reserved IP ID.
                type: string
              instanceID:
                description: InstanceID is the ID of the attached instance, if any.
                type: string
              ready:
                description: Ready denotes that the reserved IP is ready.
                type: boolean
              subnet:
                description: Subnet is the assigned IP address.
                type: string
              subnetSize:
                description: SubnetSize is the number of bits for the netmask.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
