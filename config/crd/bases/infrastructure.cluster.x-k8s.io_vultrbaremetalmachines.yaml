---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: vultrbaremetalmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: VultrBareMetalMachine
    listKind: VultrBareMetalMachineList
    plural: vultrbaremetalmachines
    singular: vultrbaremetalmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster to which this VultrBareMetalMachine belongs
      jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - description: Vultr bare metal server state
      jsonPath: .status.subscriptionStatus
      name: State
      type: string
    - description: Machine ready status
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Vultr bare metal server ID
      jsonPath: .spec.providerID
      name: InstanceID
      type: string
    - description: Machine object which owns with this VultrBareMetalMachine
      jsonPath: .metadata.ownerReferences[?(@.kind=="Machine")].name
      name: Machine
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: VultrBareMetalMachine is the Schema for the vultrbaremetalmachines
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VultrBareMetalMachineSpec defines the desired state of VultrBareMetalMachine
            properties:
              app_id:
                description: AppID is the Vultr marketplace application ID to deploy
                  on the bare metal server.
                type: integer
              image_id:
                description: ImageID is the Vultr image ID to use when deploying the
                  bare metal server.
                type: string
              ipxe_chain_url:
                description: IPXEChainURL is the URL to chain-load iPXE from when
                  booting the bare metal server.
                type: string
              mdisk_mode:
                description: MdiskMode is the RAID configuration for bare metal servers
                  with multiple disks.
                type: string
              os_id:
                description: OsID is the Vultr OS ID to use when deploying the bare
                  metal server.
                type: integer
              persistent_pxe:
                description: |-
                  PersistentPxe enables persistent PXE booting for the bare metal server.
                  When true, the server will always boot from PXE.
                type: boolean
              planID:
                description: PlanID is the id of Vultr bare metal plan.
                type: string
              providerID:
                description: ProviderID is the unique identifier as specified by the
                  cloud provider.
                type: string
              region:
                description: The Vultr Region (DCID) the cluster lives on.
                type: string
              reserved_ipv4:
                description: ReservedIPv4 is the reserved IPv4 address to assign to
                  the bare metal server.
                type: string
              script_id:
                description: StartupScriptID is the Vultr startup script ID to execute
                  on the bare metal server.
                type: string
              sshKey:
                description: sshKey is the name of the ssh key to attach to the bare
                  metal server.
                items:
                  type: string
                type: array
              vpc_id:
                description: |-
                  VPCID is the id of the VPC to be attached after creation.
                  Note: Bare metal VPC attachment is performed after server creation.
                type: string
            required:
            - region
            type: object
          status:
            description: VultrBareMetalMachineStatus defines the observed state of
              VultrBareMetalMachine
            properties:
              addresses:
                description: Addresses contains the Vultr bare metal server associated
                  addresses.
                items:
                  description: NodeAddress contains information for the node's address.
                  properties:
                    address:
                      description: The node address.
                      type: string
                    type:
                      description: Node address type, one of Hostname, ExternalIP
                        or InternalIP.
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              conditions:
                description: Conditions defines current service state of the VultrBareMetalMachine.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              cpu:
                description: CPU represents the number of CPUs of the bare metal server.
                type: integer
              failureMessage:
                description: |-
                  FailureMessage will be set in the event that there is a terminal problem
                  reconciling the Machine and will contain a more verbose string suitable
                  for logging and human consumption.

                  This field should not be set for transitive errors that a controller
                  faces that are expected to be fixed automatically over
                  time (like service outages), but instead indicate that something is
                  fundamentally wrong with the Machine's spec or the configuration of
                  the controller, and that manual intervention is required. Examples
                  of terminal errors would be invalid combinations of settings in the
                  spec, values that are unsupported by the controller, or the
                  responsible controller itself being critically misconfigured.

                  Any transient errors that occur during the reconciliation of Machines
                  can be added as events to the Machine object and/or logged in the
                  controller's output.
                type: string
              failureReason:
                description: |-
                  FailureReason will be set in the event that there is a terminal problem
                  reconciling the Machine and will contain a succinct value suitable
                  for machine interpretation.

                  This field should not be set for transitive errors that a controller
                  faces that are expected to be fixed automatically over
                  time (like service outages), but instead indicate that something is
                  fundamentally wrong with the Machine's spec or the configuration of
                  the controller, and that manual intervention is required. Examples
                  of terminal errors would be invalid combinations of settings in the
                  spec, values that are unsupported by the controller, or the
                  responsible controller itself being critically misconfigured.

                  Any transient errors that occur during the reconciliation of Machines
                  can be added as events to the Machine object and/or logged in the
                  controller's output.
                type: string
              ram:
                description: RAM represents the amount of memory of the bare metal
                  server.
                type: string
              ready:
                description: Ready represents the infrastructure is ready to be used
                  or not.
                type: boolean
              storage:
                description: Storage represents the disk size of the bare metal server.
                type: string
              subscriptionStatus:
                description: SubscriptionStatus represents the status of subscription.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
